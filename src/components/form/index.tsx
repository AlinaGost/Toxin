import * as React from 'react';
import styles from './styles.module.scss';

const Form = () => {
  return (
    <div className={styles.form}>
      <h1>Hello!</h1>
    </div>
  );
};

export default Form;
